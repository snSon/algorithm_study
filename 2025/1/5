#include <stdio.h>

int main(){
    int t = 0;
    scanf("%d", &t);

    int a = t/300;
    t -= a*300;

    int b = t/60;
    t -= b*60;

    int c = t/10;
    t -= c*10;

    if(t > 0){
        printf("-1\n");
    }
    else{
        printf("%d %d %d\n", a, b, c);
    }

    return 0;
}
